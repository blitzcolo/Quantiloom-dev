# Quantiloom 论文证据生产实验清单

对应稿件:paper.md(§VIII-D/E/G/H、Fix 5/7/8、Figs 2–10)。
总原则:**每个实验的负结果也是可写进论文的结果**——稿件里已为多数负分支预留了措辞位,所以照实报告,不要调参到"好看"为止。

---

## E0. 通用实验规约(所有实验共用,先做)

**目的:** 让每个数字可复现、可回查,填补 §VIII-A 的 EVIDENCE NEEDED(硬件、驱动、场景规模)。

**内容:**
1. 每次实验运行写一份 manifest:git commit hash、完整配置(JSON/CLI 参数)、seed(默认 `0x5EED`)、spp、分辨率、开始/结束时间。建议在 CLI 加 `--manifest out.json`,一次改动全局受益。
2. 记录一次性环境信息:GPU 型号、驱动版本、CPU 型号、内存、OS;填入 §VIII-A 与 §III。
3. 为六个场景各记录三角形数、材质数、光源数(§VIII-A EVIDENCE NEEDED)。
4. 计时类实验统一:锁定 GPU 电源模式(或至少记录),关闭无关进程,丢弃前 3 次 warm-up,报告中位数与 p95,重复 N≥30。

**边界:** manifest 不必进论文,但审稿人质疑任何数字时,你要能在一天内重放。

---

## E1. 跨渲染器对比(§VIII-D,投稿门槛)

**目的:** 全文唯一的第三方传输核验证;Table I 中 Quantiloom 行的外部证据。

**实验设计(展开):**
反射域三个场景,**基准分两类**——这是对稿件协议的一处修正,先改协议再跑:
- **白炉(white furnace)**:封闭腔,均匀入射辐亮度 L₀,朗伯材质;解析期望值存在(能量平衡闭式解)。两个渲染器**各自对解析值**报偏差,不互拍。
- **开天空等价**:开放场景对闭式天空辐亮度;同上,各自对解析值。
- **Cornell 间接光**:无解析参考,用跨渲染逐像素比较:δ(x) = |L_QL − L_M3| / max(L_M3, ε_floor) 与全帧 RMSE。

对照渲染器:Mitsuba 3,`scalar_spectral` variant。场景移植要点:
- **材质只用朗伯漫反射 + 面积光**。禁用任何微表面 lobe——GGX 粗糙度参数化、多次散射补偿在两个渲染器约定不同,会把参数化差异冒充成物理分歧。传输核验证不需要复杂材质。
- 光谱通过 .spd 文件逐波长对齐(不要让 Mitsuba 从 RGB 上采样——那会引入它自己的上采样模型差异)。
- 相机参数、胶片、无 tone mapping、线性 HDR(EXR)输出,逐一核对。
- 波长积分域对齐:两边使用相同的 λ 范围与(若可配置)相同的谱带离散。

**步骤:**
1. 修订 §VIII-D 协议文字(解析参考优先)。
2. 从发布版不变量场景定义(§VII-6 releasable set)导出几何 + 光谱到 Mitsuba XML;写转换脚本入 `scripts/validation/`(现为空占位,正好补上)。
3. 双方渲染到 MC 噪声地板远低于比较阈值:先跑 spp 阶梯(256/1k/4k/16k)确认收敛,报告最终 spp 与残余噪声(半样本分裂估计)。
4. 白炉、开天空:报告各自对解析值的平均/最大相对偏差。Cornell:δ(x) 热图 + RMSE;排除 L < 地板值的像素并声明地板值。
5. Stretch:Mitsuba blackbody 面发射体拼等温腔。只做 ε=1 构型(Mitsuba 面光不做 Kirchhoff 一致的 ε<1 自发射);跑通报告一致性,跑不通写入预备好的"第三方不可跑"声明。

**预期结果:**
- 正:解析场景双方偏差 <1%(受谱离散与天空模型等价性限制);Cornell RMSE 在 1–2% 量级(受 MC 噪声与几何移植精度限制)。
- 负分支 A:系统性常数偏移 → 大概率单位/归一化约定(sr、nm、球面度量),排查后属移植问题,不进论文正文。
- 负分支 B:排除移植因素后仍有真实分歧 → **这正是锚点存在的意义**,当作 gate 处理:修渲染器或如实报告分歧及归因。不允许第三种状态。

**注意事项与边界:**
- 本实验只验证反射域标量传输;不验证热学、传感器、自发射(后者由 stretch 或声明覆盖)。
- Cornell 逐像素比较对几何移植误差极敏感(墙缝、光源尺寸);用双方各自渲染的 AOV(直接光/间接光分离,若有)辅助定位分歧来源。
- 引用债:补 Mitsuba 3 正式引用(§VIII-D CITATION NEEDED)。

---

## E2. 站点强迫能量平衡验证(§VIII-H,投稿门槛)

**目的:** 能量平衡核(贡献 2 的载体)对实测数据的锚定;全文唯一实测证据。

**实验设计(展开):**
- **站点**:SURFRAD 网络,首选 **Desert Rock, NV**(裸土、干旱、晴空日多、与 desert 场景语义一致);备选 Table Mountain。数据公开(NOAA FTP/HTTPS),1 分钟分辨率:下行/上行短波与长波、气温、湿度、风速。
- **"实测地表温度"** 由上行长波反演:T_s = [(LW↑ − (1−ε)·LW↓) / (εσ)]^{1/4},宽带 ε 取裸土文献值(0.96–0.97),**做 ε±0.02 敏感性并把结果温度带宽写进论文**——这是协议里 "documented, stated emissivity assumption" 的兑现。
- **两组驱动模式**(先做代码路径检查):
  - (a) 实测 LW↓ 直接注入 → 纯求解器误差;
  - (b) 气温/湿度经 Berdahl–Fromberg 关联生成天空长波 → 求解器 + 晴空模型合成误差。
  - (b)−(a) 分解出关联式误差,§V.3 的晴空模型白得一次独立站点验证。**若强迫 CSV 不支持直接注入 LW↓,先加这个输入通道(小改动),否则 (a) 做不了,论文只能报 (b) 并声明混合。**
- **表面柱**:裸土热物性取文献值(干土 κ≈0.3–1.0 W/m·K,体积热容≈1.2–1.6 MJ/m³·K);板厚 ≥ 3× 日循环趋肤深度(干土 α≈5×10⁻⁷ m²/s → 趋肤深度≈0.12 m,取 0.4–0.5 m),底边界深层恒温或绝热并检验不敏感;节点数做一次加密自查(节点翻倍结果不变)。
- **窗口**:≥3 个多日晴空窗(用实测 SW↓ 对晴空包络筛选),跨季节更好;避开降水后时段(wetness_factor=0)。
- **防事后拟合条款(重要)**:若需在文献区间内调热物性,只允许在**一个标定窗**上调,其余窗为 held-out 评估窗;论文写明这一划分。归因候选(潜热、土壤湿度、横向传导)已在稿件预注册,不得赛后新增。

**步骤:**
1. 核查/添加 LW↓ 注入路径;写 SURFRAD→八列 CSV 转换脚本。
2. 选窗(晴空筛选脚本),每窗前置 24–48 h spin-up 消初值。
3. 跑 (a)(b) 两组 × 各窗;ε 敏感性重跑一遍。
4. 产出:Fig 10 轨迹对比图(预测 vs 反演,昼夜着色);Table VIII 填行(站点、日期、昼/夜 RMSE 与 MAE、相位滞后)。相位滞后用互相关峰或过峰时刻差。

**预期结果:**
- 正:昼 RMSE 2–5 K、夜 RMSE 1–3 K、相位滞后 <1 h,对一个文献物性 1-D 柱是体面结果;(a) 优于 (b),差值即关联式贡献。
- 负分支 A:白天系统性偏热 → 潜热/土壤湿度(预注册归因),如实写。
- 负分支 B:振幅系统性偏差 → 热惯量取值,走标定窗/评估窗流程。
- 负分支 C:夜间明显劣于白天 → 与 Joseph 1998 的 0.52 先例同构,反而是可讨论的一致性发现。

**注意事项与边界:**
- 本实验验证 1-D 柱 + 通量项;**不验证**视因子互换(开阔平坦站点,天空主导)、不验证渲染与传感器。稿件已写明,勿在结论里越界。
- SURFRAD 上行长波含仪器视场内的地表非均一性;选站时看站点照片确认地表均质。
- 数据引用:SURFRAD 数据集需正式引用与致谢(NOAA 要求)。

---

## E3. 交互性计时 + 补充视频(§VIII-G,标题词的证据)

**目的:** 给标题第一个词 "Interactive" 三个可检验数字;顺产 §VIII-A 硬件信息与补充视频。

**实验设计(展开):**
三个 target(稿件已定):
- T1 几何编辑 → TLAS refit → 首个渐进帧 ≤ 0.5 s;
- T2 时间轴回拖 ≤ 2 s(默认 checkpoint stride,desert 场景);
- T3 渐进收敛 RMSE–时间曲线(RMSE 定义沿用 §VIII-B)。

计时点插桩在 RenderCore 层而非 GUI 事件层(避免 Qt 事件队列噪声);若可行,用 CLI/测试 harness 驱动同一 RenderCore 做脚本化重复(cross-validation-by-construction 的又一次利用)。

**步骤:**
1. 插桩三条路径:drag→refit→present、seek→restore→re-solve→纹理更新、accumulate→RMSE。
2. T1:脚本化 100 次随机小幅位移,报告中位数/p95;区分 refit(拖拽中)与 full rebuild(松手)两个数字。
3. T2:stride 扫描 {0.25, 0.5, 1, 2} h × 各 30 次随机回拖,画 latency–stride 曲线(内存–延迟权衡的显式呈现);另测 lazy invalidation:时间轴中段改参数后首次回拖的额外成本单列。
4. T3:desert 与图库场景各一条 RMSE–时间曲线,对 16k spp 参考帧。
5. 视频:OBS 60 s 录屏——回拖时间轴(热阴影随时刻移动)→ gizmo 拖球(阴影跟随+渐进收敛)→ 静置收敛;叠加时间戳。
6. 填 §VIII-A/III 硬件与驱动信息。

**预期结果:**
- 正:T1 大概率大幅达标(TLAS refit 为毫秒级,1 spp 首帧几十毫秒);T2 取决于 stride 与 GPU 步进吞吐,默认 stride 下应 <2 s。
- 负分支:某 target 未达 → 按稿件条款**报告数字 + 归因**(如 re-solve 段主导),不放宽 target。这句承诺已写进论文,violating it 比 miss target 更伤。

**注意事项与边界:**
- 计时对 GPU 频率状态敏感:锁频或记录电源模式;报告环境。
- T2 的"默认 stride"要与代码默认值一致并在文中写出数值。
- 视频里出现的一切能力必须与论文声明一致(不要演示论文没写的功能)。

---

## E4. dT/dv 网格加粗扫描 + 阴影边缘误差剖面(Fix 7 + Fig 3/7,核心贡献的直接证据)

**目的:** 把"网格加密需 2–3 个数量级"从估计变成实测曲线;产出 Fig 3(有/无校正对)与 Fig 7(边缘误差剖面)。这是贡献 2 对 Lagouarde 一线优势的**唯一直接演示**。

**实验设计(展开):**
- **真值构造(关键)**:利用模型自身的结构性事实——每元素是独立 1-D 柱 + 可见度历史,无横向耦合(desert 开阔场景,视因子近天空主导)。因此逐采样点的精确参考 = 以该点自己的追踪可见度**时间序列**驱动独立 1-D 柱积分。写一个参考脚本:沿横穿阴影边缘的样带取 N≈200 个采样点,逐点追踪全天可见度序列,逐点积分柱模型 → 精确 T_ref(x)。这个真值成本 O(N),与网格无关。
- **扫描**:地面网格 51²、101²、201²、401²(401² 若视因子成本过高,截断 top-K 或禁用互换并声明——注意与真值假设一致)。固定评估时刻(如 15:00,阴影充分发育)。
- 每档网格输出两张温度场:未校正(逐元素常值)与 dT/dv 校正;沿同一样带取值,对 T_ref 计算误差。
- **指标**:样带最大误差、边缘 ±2 m 带内 RMSE;横轴三角形边长,双对数。附 break-even 分析:未校正误差降到校正水平所需的元素数(实测外推,替换稿中"2–3 个数量级"估计)。

**步骤:**
1. 写逐点柱参考脚本(可复用 CPU 步进器,逐点喂可见度序列)。
2. 生成四档网格的场景配置;逐档跑昼间求解 + 两种渲染。
3. 提样带、算误差、画 Fig 7(误差剖面,双线:有/无校正)与加粗标度曲线(新图或并入 Fig 7)。
4. Fig 3 从 201² 档渲染对(a)(b) 出图:同视角、同色标、色标单位 K。

**预期结果:**
- 正:未校正边缘误差随三角形尺寸线性增长(上限逼近全对比 28.9 K);校正误差平坦钉在 ~1 K(3.5%)不随网格变化——两条线的剪刀差就是论文的核心图。
- 负分支 A:校正误差在深阴影处超过 3.5% → 一阶线性化在大 Δv + 长历史分歧下的边界,与已做的 full-shadow trajectory 验证呼应,如实画出并在 §V 边界段落引用。
- 负分支 B:真值与 201² 元素均值不闭合 → 检查 v_element 语义(质心二值 vs 分数覆盖),顺带解决稿中 "exact at element means" 措辞核实项。

**注意事项与边界:**
- 点光源太阳无真实半影;图注措辞用 "shadow-edge transition band",除非渲染器太阳有角尺寸(有则注明 0.5° 对应的物理半影宽度)。
- 真值假设"无互耦"须与各档网格的求解配置一致,否则真值失效;在文中一句话声明该假设及其在 desert 场景的合理性。
- 校正射线沿太阳方向偏置等实现细节不在本实验验证范围(单测已覆盖)。

---

## E5. 热像往返曲线 + NETD 曲线 + FPN 注入-残存(§VIII-E)

**目的:** 把三个"机制已存在于测试中"的断言变成出版曲线。

**实验设计(展开):**
- **(a) 逐波段往返曲线**:8 个腔构型 × 腔温扫描 250–350 K(步长 10 K),渲染→反演,画 |T_rec − T_set| 对 T 与对波段两张曲线。
- **(b) NETD 曲线**:黑体场景 T ∈ [250, 350] K,固定传感器配置(积分时间、F 数、QE、井容,全部写进图注)。双轨:解析 NETD(式 9)+ **经验 NETD**——同配置渲染 N=100 帧(seed 逐帧变化),逐像素 DN 时序标准差经 dL/dT 折算到温度,取像素中值。两条曲线同图。
- **(c) FPN 注入-残存**:注入已知 PRNU σ_p ∈ {0.5%, 1%, 2%}(乘性)与 DSNU(加性);NUC 效率 {95%, 97%, 99%};亮场与暗场各平均 ≥200 帧压掉时序噪声,残余空间标准差即残存 FPN。验证:残存 ≈ (1−eff)×注入;暗场只见 DSNU、亮场见二者,证明分离建模的可标定性。

**步骤:** 三个子实验各写一个批处理脚本(CLI batch 模式正合适);产出 §VIII-E 两曲线 + 一表(或并入 Fig 5)。

**预期结果:**
- 正:(a) 误差 ≪0.1 K 且平坦(同一积分核往返,残差应为数值精度级;若随波段有结构 → 反演插值实现问题,先修后报);(b) NETD 随 T 上升而下降(dL/dT 增长),解析与经验吻合到几个百分点——这个吻合本身就是 responsivity 一致性断言的实验化;(c) 残存与 (1−eff) 线性,亮暗场行为分离。
- 负分支:(b) 解析≉经验 → responsivity 或噪声模型 bug,正是断言要抓的东西;修复后报告(可作为 gate 拦截记录的新条目)。

**注意事项与边界:**
- (b) 排除 FPN 与饱和(NETD 定义的两个 deliberate exclusion),配置里显式关断并在图注声明。
- (c) 的注入强度与 NUC 效率组合数别贪多,3×3 足够画清线性。

---

## E6. Furnace 表逐行数据(Table IV,半天)

**目的:** 把 "1.0000(打印精度)" 变成逐构型实数比值 + 显式容差。

**步骤:**
1. 给 `run_furnace_suite.sh` 输出加位数(打印 6–8 位有效数字;纯输出改动,注明不改物理)。
2. 跑全套,记录:构型名、波段、腔温、壁 ε(若构型间有差异)、谱模式、spp、比值、断言容差。
3. Table IV 填 8 行,表注报告 max |ratio − 1|(如 <1×10⁻⁴)与容差来源。

**预期:** 正:偏差在 MC 噪声 + 谱积分离散决定的地板上。负:某构型偏差显著大于其余 → 该构型参数落在积分域边界(如腔温 Planck 峰移出波段),查明后要么修要么在表注解释。

---

## E7. 图库资产与四联图(Fig 8,最大隐性风险,今天启动)

**目的:** 五波段条带、昼夜阴影序列、伪装融合、ENVI 立方体——"cross-band" 的感官证据。

**实验设计(展开):**
- **先做资产盘点(半天,决定后续一切)**:候选主场景需满足——视觉拿得出手;材质在五个波段均有实测谱覆盖(逐波段 coverage 数字可引用,IX 的 55% 声明与之呼应);含至少一个热学上有趣的物体(投影、遮蔽)。若现有场景不满足,资产制作(建模/贴图/谱绑定)可能比任何实验都长,须最先排期。
- 五波段条带:同场景同相机,VIS(sRGB)/NIR/SWIR/MWIR/LWIR(线性 AGC),逐 panel 标注显示变换——**任何定量 panel 禁用 CLAHE**(§VI 自己论证过的)。
- 昼夜序列:`thermal_sequence/` 批路径,取 4–6 个时刻,展示 dT/dv 校正后阴影边缘随太阳移动。
- 伪装融合:VIS 中不可分、热波段可分的目标;Laplacian 金字塔融合;图注声明融合输出无物理单位(稿中已写)。
- ENVI 立方体:渲染立方体 + 第三方工具(ENVI/QGIS/spectral-python)打开截图,证明互操作;图注声明 qualitative。

**步骤:** 盘点 → 缺什么补什么(材质谱绑定优先于建模美化)→ 渲染(≥300 dpi 目标版面宽度)→ 拼版。

**预期:** 正:一图让三个社区的审稿人各看到自己认的东西(反射→自发射过渡、阴影物理、数据产品)。负分支:某波段材质覆盖不足导致画面"平" → 宁可换场景,不可临时伪造谱(会与 IV 章的禁止语义自相矛盾)。

**注意:** 所有 panel 的 seed、spp、显示变换写进图注或补充材料;这张图会被最多人看,也最容易被挑显示处理的毛病。

---

## E8. 剩余图件与琐碎项(穿插完成)

- **Fig 2**(SheenChair 带外曲线):取 mango velvet 的 (c₀,c₁,c₂),画 R(λ) 380 nm–12 µm(对数横轴),标注 [380,780] 钳制区与 1.2 µm/10 µm 两个数据点(0.9998/1.0000)。纯画图,半天。
- **Fig 1/4/6**:管线、传感器链、四门流程示意图;工具随意,风格统一。
- **Fig 5**:E5(c) 的链条累积面板 + 线性 AGC vs CLAHE 对照(附 50.6% 反序对统计;3.16 MP 场景重跑确认该数字)。
- **Table V(a) 三个空格**:回查 git log/构建日志确定"哪个门抓的";查不到就把表列改为不做逐缺陷门归属(空格不可留)。
- **仓库发布**:physics-audit harness、9 个自洽脚本、反射域不变量场景(E1 的移植脚本一并放入);拿到 URL 填 §VII 与 Data Availability。
- **两处措辞核实**:① 1.85× 上限是在哪张方差图上算的(白噪声 or 分层后)——决定 "captures 94%" 的表述是否成立;② v_element 语义(质心二值 or 分数)——决定 "exact at element means" 的措辞(E4 负分支 B 顺带解决)。

---

## 执行顺序与依赖

| 周 | 并行线 A(渲染/传输) | 并行线 B(热学) | 并行线 C(资产/图) |
|---|---|---|---|
| 1 | E0 规约 + E1 场景移植与协议修订 | E2 LW 注入路径检查 + 选站选窗 | **E7 资产盘点(第一天)**+ 缺口制作启动 |
| 2 | E1 跑数 + E6 furnace 表 | E2 跑数 + E4 真值脚本 | E7 渲染;E8 Fig 2 |
| 3 | E5 三条曲线 + E3 插桩计时 | E4 扫描与出图 | Fig 1/4/6 示意图;E3 视频 |
| 4 | 机动:负分支处理、仓库发布、全文数字回填、"remains to be produced" 全局替换、Outstanding/EVIDENCE NEEDED 清除 | | |

依赖关系:E1 依赖协议修订;E4 依赖真值脚本;E3 视频依赖 E7 的场景可看;其余互相独立。阅读债(CAMEO-SIM、Cathcart、MuSES/registry、Jakob–Hanika 全文)塞在渲染等待间隙,与实验无依赖。
