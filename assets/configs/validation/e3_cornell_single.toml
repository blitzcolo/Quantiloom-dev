# Auto-generated by scripts/validation/run_e3_cornell.py.
# The Cornell box at one wavelength, for cross-renderer comparison. No spectral
# basis file: the materials carry their measured reflectance through the glTF's
# QUANTILOOM_material_ir extension, and the comparison binds the same CSVs on
# the Mitsuba side, so both renderers read one file per material.

[renderer]
resolution = [512, 512]
spp = 4096
seed = 21628
output = "renders/validation/cornell/ql_550.exr"

[spectral]
mode = "single"
wavelength_nm = 550.0

[scene]
gltf = "H:/Quantiloom-dev/assets/models/cornell_box/cornell_box.gltf"
world_units_to_meters = 0.001
default_temperature_k = 300.0

[camera]
position = [278.0, 273.0, -800.0]
look_at = [278.0, 273.0, 280.0]
up = [0.0, 1.0, 0.0]
fov_y = 39.0

[lighting]
sun_direction = [0.0, 1.0, 0.0]
sun_radiance = [0.0, 0.0, 0.0]
sky_radiance = [0.0, 0.0, 0.0]

[material]
albedo = [0.5, 0.5, 0.5]

[sensor]
enabled = false

[quality]
log_material_sources = true

[spectral_curves]
"Construction Concrete" = "H:/Quantiloom-dev/build/validation/cornell_m3/concrete_visible.csv"
"Red Brick" = "H:/Quantiloom-dev/build/validation/cornell_m3/brick_visible.csv"
"Olive Green Paint" = "H:/Quantiloom-dev/build/validation/cornell_m3/paint_visible.csv"
"White Marble" = "H:/Quantiloom-dev/build/validation/cornell_m3/marble_visible.csv"
"Asphalt" = "H:/Quantiloom-dev/build/validation/cornell_m3/asphalt_visible.csv"

[material_overrides."Construction Concrete"]
specular = 0.0

[material_overrides."Red Brick"]
specular = 0.0

[material_overrides."Olive Green Paint"]
specular = 0.0

[material_overrides."White Marble"]
specular = 0.0

[material_overrides."Asphalt"]
specular = 0.0

[material_overrides."Light Panel"]
specular = 0.0
